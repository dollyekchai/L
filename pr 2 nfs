sudo su
sudo apt-get install  nfs-kernel -server
sudo nano /etc/idmapd.conf
sudo nano /ect/exports
systemctl restart nfs-server
mont -t nfsdlp.srv.world:/home/home
dg -hT
sudo nano /etc/fstab
sudo apt-get install autofs
sudo nano /etc/auto.master
sudo mkdir /mntdir1
systemctl restart autofs
cd /mntdir1
cat /proc/mount | grep mntdir1

now open control panel in  windows enable NFS server
