ifconfig
ip r | grep default
sudo nano /etc/network/interfaces

address 10.0.2.2
netmask 255.0.0.0
gateway 10.0.2.255

reboot
sudo /bin/su -c “echo ‘net.ipv6.conf.all.disable_ipv6=1’ 
>>/etc/sysctl.conf” 
sudo sysctl -p
ipmaddr
systemctl -t service
systemctl stop apparmor
systemctl disable apparmor 
